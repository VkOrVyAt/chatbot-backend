DATABASE_URL=
# The database URL should be in the format:
# postgresql://<username>:<password>@<host>:<port>/<database_name>
# The secret key is used for cryptographic signing and should be kept secret.
# You can generate a secret key using Python:
# import secrets
# print(secrets.token_urlsafe(16))
# The .env file should not be committed to version control for security reasons.
# Make sure to create a .env file in the root directory of your project and fill in the values.
# The .env file should not be committed to version control for security reasons.    
SECRET_KEY=
JWT_ALGORITHM=
JWT_EXPIRATION_TIME=